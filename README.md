# Hermes Application

This repository contains the public application logic for Hermes.

## Backup & Restore
For backup and restore procedures, refer to `scripts/backup.sh` and `scripts/restore.sh`.

## Configuration
See `config.example.yaml` for configuration options and `.env.example` for environment variables.
